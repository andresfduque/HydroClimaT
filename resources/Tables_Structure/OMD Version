Table: EOMD Version

The Methods table lists the methods used to collect the data and any additional information about
the method.

CREATE TABLE EOMDVersion (
	VersionNumber varchar(50) not null,
	PRIMARY KEY (VersionNumber)
	);

